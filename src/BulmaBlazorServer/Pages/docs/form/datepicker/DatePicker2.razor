dt:<span>@(dt1?.ToString("yyyy-MM-dd")) </span><br>
<DatePicker @bind-Value="dt1" Options="opt"></DatePicker>

@code {
    DateTime? dt1;

    DatePickerOptions opt = new()
    {
        Format = "HH:mm",
        StartView = 0,
        PickTime = true
    };

}