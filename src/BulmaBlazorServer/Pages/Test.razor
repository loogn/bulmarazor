@inject IJSRuntime js

@code{
    private string str = "23d";

    bool? b=true;
    DateTime? dateTime=DateTime.Now;
    HashSet<int> list=new HashSet<int>{1,2};
    private int i = 2;
    async Task clk(){

        await js.InvokeVoidAsync("BulmaRazor.Log",str);
        str+="s";
        i+=1;
        b=!b.Value;
        dateTime=dateTime.Value.AddDays(1);
    }
}