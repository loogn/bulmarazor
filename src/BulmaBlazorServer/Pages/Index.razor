@page "/"
@inject JSWindow win


<Hero Size="Size.Medium" Color="Color.Primary" IsBold IsFullheightWithNavbar>
    <BodySlot>

        <Container>
            <Title Size="Size.Size1">
                BulmaRazor
            </Title>
            <Subtitle Size="Size.Size3">
                BulmaRazor 是一个基于 Blazor 和 Bluma 样式框架构建的弹性组件库。
            </Subtitle>

            <Buttons>
                <Button href="https://gitee.com/loogn/bulmarazor" target="_blank" Color="Color.Info" IsOutlined IsInverted Size="Size.Large">在Gitee上访问</Button>
                <Button href="https://gitee.com/loogn/bulmarazor" target="_blank" Color="Color.Primary" IsOutlined IsInverted Size="Size.Large">在Gitee上访问</Button>
            </Buttons>
        </Container>
    </BodySlot>

</Hero>




@code{
    string str = "11";
    int i = 0;
    
    bool _ck;

    List<string> values = new List<string> { "11", "22", "33", "44" };
    HashSet<string> list = new HashSet<string> { "22" };

    void CH(ChangeEventArgs e)
    {
        win.alert(e.Value.ToString());
    }

    void f1()
    {
        str = "22";
    }
    void f2()
    {
        list.Add("22");
    }
    void Up(InputFileChangeEventArgs e)
    {
        win.alert(e.FileCount.ToString());
    }

}