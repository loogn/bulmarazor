@namespace BulmaRazor.Components

@inherits BulmaComponentBase

    <progress @attributes="Attributes" class="@classes">@ChildContent</progress>
@* } *@
@* else *@
@* { *@
@*     <progress value="@Value" max="@Max" @attributes="Attributes" class="@classes">@ChildContent</progress> *@
@* } *@

@code {

    string classes => CssBuilder.Default("progress")
        .AddClassFromAttributes(Attributes)
        .AddClass(Color.Value, Color.Value)
        .AddClass(Size.Value, Size.Value)
        .Build();

    [Parameter]
    public Color Color { get; set; } = Color.Default;

    [Parameter]
    public Size Size { get; set; } = Size.Default;

    private double? _value=null;

    // [Parameter]
    // public double? Value
    // {
    //     get => _value;
    //     set
    //     {
    //         _value = value;
    //     }
    // }
    //
    // [Parameter]
    // public double? Max { get; set; }

    [Parameter]
    public RenderFragment ChildContent { get; set; }
    
}